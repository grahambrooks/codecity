apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: codecity
  labels:
    app.kubernetes.io/name: codecity
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: codecity
    app.kubernetes.io/component: backend
